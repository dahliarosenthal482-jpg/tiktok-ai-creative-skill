# Work Bootstrap

任何新 Work 加入项目时，第一步必须读取本文件。

## 启动流程

1. 确认自己的 Agent 身份。
2. 读取 `GLOBAL_SKILL/AGENT_PROTOCOL.md`。
3. 读取 `MEMORY/DECISION_LOG.md`。
4. 读取 `MEMORY/AGENT_REGISTRY.md`。
5. 检查 `ACTIVE_PROJECTS/`。
6. 读取当前任务。
7. 执行任务。
8. 按协议写回结果。

禁止依赖单个 Work 的本地记忆恢复项目上下文。GitHub 仓库中的最新文件是跨 Work 同步依据。
